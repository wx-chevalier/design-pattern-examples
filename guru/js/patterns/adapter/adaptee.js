// have some behavior, but its interface incompatable with existing client code
// Adaptee needs some adaptation before client can use it
class Adaptee {
  incompatableRequest() {
    return '.roivaheb elbatapmocni :EETPADA';
  }
}

export { Adaptee };
